Drop your photo files here (.jpg, .jpeg, .png, or .webp) — around 200 is
totally fine. No naming convention needed; the app picks one at random each
time a quote/tip/spotlight post uses the photo-card style instead of the
gradient card.

This file just keeps the folder from disappearing when zipped/committed
while it's still empty — delete it once you've added real photos, or leave
it, it won't be picked up as a photo since it's not an image file.
